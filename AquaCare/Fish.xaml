<Page x:Class="AquaCare.Fish"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AquaCare"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Fish" Background="#FFE9E9E9">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl x:Name="FishItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:SpeciesCard 
                        CardTitle="{Binding Name}"
                        CardDescription="{Binding Description}"
                        CardImagePath="{Binding CoverPath}"
                        MouseLeftButtonDown="SpeciesCard_MouseLeftButtonDown" Cursor="Hand"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
