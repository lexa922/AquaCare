<Page x:Class="AquaCare.CreatingAquarium"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AquaCare"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Створити Акваріум" Background="#FFE9E9E9"
      Loaded="Page_Loaded">
    <Page.Resources>
        <local:StringToBrushConverter x:Key="StringToBrushConverter"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel HorizontalAlignment="Center" Width="500" Margin="20">

            <Label Content="Додавання нового акваріума" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <GroupBox Header="Основні параметри" Padding="10" Margin="0,0,0,15" Background="White" BorderBrush="#CCCCCC">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="Назва:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="TankName" Grid.Row="0" Grid.Column="1" Margin="5" Height="25"/>

                    <Label Content="Об'єм (літри):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                    <xctk:IntegerUpDown x:Name="TankVolume" Grid.Row="1" Grid.Column="1" Margin="5" Minimum="1" Value="50"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Режим освітлення" Padding="10" Margin="0,0,0,15" Background="White" BorderBrush="#CCCCCC">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="Ввімкнення:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <xctk:TimePicker x:Name="LightOnTime" Grid.Row="0" Grid.Column="1" Margin="5" Format="ShortTime" Value="09:00"/>

                    <Label Content="Вимкнення:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                    <xctk:TimePicker x:Name="LightOffTime" Grid.Row="1" Grid.Column="1" Margin="5" Format="ShortTime" Value="21:00"/>
                    <Label Content="Інтенсивність:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
                    <ComboBox x:Name="LightIntensityList" Grid.Row="3" Grid.Column="1" Margin="5"
                  DisplayMemberPath="Name" 
                  SelectedValuePath="LightIntensityLevelId"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Субстрат (Грунт)" Padding="10" Margin="0,0,0,15" Background="White" BorderBrush="#CCCCCC">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ListBox x:Name="SubstrateList" 
         Height="220" 
         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
         ScrollViewer.VerticalScrollBarVisibility="Auto"
         Background="Transparent"
         BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <local:SubstrateCard 
                Title="{Binding Name}" 
                Description="{Binding Description}" 
                ColorBrush="{Binding Color, Converter={StaticResource StringToBrushConverter}}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </GroupBox>

            <Button Content="Створити акваріум" Height="40" FontSize="14" FontWeight="Bold" 
                    Background="#0080FF" Foreground="White" Margin="0,10,0,0"
                    Click="SaveButton_Click"/>

        </StackPanel>
    </ScrollViewer>
</Page>