<UserControl x:Class="AquaCare.SubstrateCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AquaCare"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="400" Width="300"
             x:Name="RootSubstrateCard">

    <Border BorderBrush="#DDDDDD" 
            BorderThickness="1" 
            CornerRadius="4" 
            Background="White" 
            Margin="2"
            Height="60">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                    Margin="5"
                    CornerRadius="3"
                    Background="{Binding ColorBrush, ElementName=RootSubstrateCard}"
                    BorderBrush="Gray" 
                    BorderThickness="1"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0">

                <TextBlock Text="{Binding Title, ElementName=RootSubstrateCard}" 
                           FontWeight="Bold" 
                           FontSize="14"
                           TextTrimming="CharacterEllipsis"/>

                <TextBlock Text="{Binding Description, ElementName=RootSubstrateCard}" 
                           FontSize="12" 
                           Foreground="Gray"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
