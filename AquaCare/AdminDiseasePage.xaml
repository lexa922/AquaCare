<Page x:Class="AquaCare.AdminDiseasePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AquaCare"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Конструктор Хвороб" Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="🛠️ Створення нової хвороби" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Назва хвороби:"/>
            <TextBox x:Name="DiseaseNameBox" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Методи лікування:"/>
            <TextBox x:Name="TreatmentBox" Height="150" TextWrapping="Wrap" AcceptsReturn="True" 
                     Margin="0,0,0,20" Padding="5" VerticalScrollBarVisibility="Auto"/>

            <Button Content="ЗБЕРЕГТИ ХВОРОБУ" Click="SaveDisease_Click" 
                    Height="50" Background="#0080FF" FontWeight="Bold" FontSize="14"/>
        </StackPanel>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="📋 Виберіть симптоми" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="5" Background="#F0F0F0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewSymptomBox" Padding="5"/>
                    <Button Grid.Column="1" Content="+ Додати" Click="AddSymptom_Click" Margin="10,0,0,0" Padding="10,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1">
                <ScrollViewer>
                    <ItemsControl x:Name="SymptomsListControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" 
                                          IsChecked="{Binding IsSelected}" 
                                          Margin="5,5,5,5" FontSize="14"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
