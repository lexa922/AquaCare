<UserControl x:Class="AquaCare.AquariumCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="350"
             x:Name="RootCard">

    <Border Background="White" CornerRadius="10" Margin="5"
            BorderBrush="#E0E0E0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#AA000000" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="10,10,0,0" Height="45">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2196F3" Offset="0"/>
                        <GradientStop Color="#4FC3F7" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <TextBlock Text="{Binding AquariumName, ElementName=RootCard}" 
                               Foreground="White" FontWeight="Bold" FontSize="18"
                               VerticalAlignment="Center" Margin="15,0"/>
                    <TextBlock Text="⚙️" Foreground="White" HorizontalAlignment="Right" 
                               VerticalAlignment="Center" Margin="0,0,15,0" Cursor="Hand" Opacity="0.8"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="💧" Width="20" TextAlignment="Center" ToolTip="Об'єм"/>
                        <TextBlock Text="{Binding VolumeText, ElementName=RootCard}" FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="⛰" Width="20" TextAlignment="Center" ToolTip="Тип ґрунту"/>
                        <TextBlock Text="{Binding SubstrateText, ElementName=RootCard}" FontSize="14" Margin="8,0,0,0" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="💡" Width="20" TextAlignment="Center" ToolTip="Режим світла"/>
                        <TextBlock Text="{Binding LightSchedule, ElementName=RootCard}" FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock Text="🔄" Width="20" TextAlignment="Center" ToolTip="Рекомендована частота підмін"/>
                        <TextBlock Text="{Binding WaterFreqText, ElementName=RootCard}" FontSize="14" Margin="8,0,0,0" FontWeight="SemiBold" Foreground="#00695C" VerticalAlignment="Center"/>
                    </StackPanel>

                </StackPanel>

                <Border Grid.Column="1" Width="60" Height="60" CornerRadius="30" Background="#F0F8FF" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="🦈" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                </Border>
            </Grid>

            <Border Grid.Row="2" BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Padding="15,8">
                <StackPanel Orientation="Horizontal">
        
        <TextBlock Foreground="Gray" Margin="0,0,15,0">
            <Run Text="Риб: "/>
            <Run Text="{Binding FishCountText, ElementName=RootCard}" FontWeight="Bold"/>
        </TextBlock>

        <TextBlock Foreground="Gray">
            <Run Text="Рослин: "/>
            <Run Text="{Binding PlantCountText, ElementName=RootCard}" FontWeight="Bold"/>
        </TextBlock>
        
        <TextBlock Text="{Binding StatusText, ElementName=RootCard}" 
                   Foreground="{Binding StatusColorBrush, ElementName=RootCard}"
                   FontSize="12" FontWeight="Bold" 
                   HorizontalAlignment="Right" Width="130" TextAlignment="Right"/>
    </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>